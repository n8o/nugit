# Re-shaped plan position (stand-in for Beads until the plan epic lands).
# Mirrors PLAN.md. `nugit pr-render` reads this for the plan-position delta.
completed:
  - "S0: spike (file→component mapping + significance, bootstrapped on nugit itself)"
  - "K1: thin keystone — nugit pr-render (deltas + consistency + significance)"
current: "K2: harden keystone (tests, GitHub Action, cost budget)"
remaining:
  - "I1: one fitness-function backend (go-arch-lint) when a 2nd language appears"
  - "I2: FTS index + retrieval context() when grep gets slow"
  - "I3: content-addressing + merge driver when concurrent-write pain is observed"
note: "Infra (index, vectors, merge driver) is conditional — built only when its trigger fires. See PLAN.md."
