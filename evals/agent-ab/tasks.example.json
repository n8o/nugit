[
  {
    "id": "example-add-endpoint",
    "prompt": "Add a read-only HTTP endpoint GET /api/widgets/:name/health to the backend that reports the widget's registration status. Follow the existing route patterns. Commit your work when done.",
    "path": "apps/backend/src/routes/widgets.ts",
    "base": "origin/main",
    "verify": "npx vitest run apps/backend/src/routes/widgets.test.ts"
  },
  {
    "id": "example-cross-service-change",
    "prompt": "Service B needs to receive a second input stream from service A. Wire it up using the project's standard inter-service transport. Commit your work when done.",
    "path": "services/b/src/b.cpp",
    "base": "origin/main",
    "verify": ""
  }
]
